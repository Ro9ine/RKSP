[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.buildArgs]
VITE_API_URL = "https://${{workflow-api.RAILWAY_PUBLIC_DOMAIN}}"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
